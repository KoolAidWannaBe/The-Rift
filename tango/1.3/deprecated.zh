// tango.zh
// Version 1.3.2

void Tango_ActivateMenu(bool canCancel)
{
    if(canCancel)
        Tango_SetMenuFlags(TANGO_MENU_CAN_CANCEL);
    else
        Tango_SetMenuFlags(0);
    Tango_ActivateMenu();
}
